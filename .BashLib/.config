function safe_source() { echo eval " \
  if [ -r $1 ] ; then \
    source $1 ; \
  else \
    logger -t $0 -p crit \"unable to source $1\" ; \
    exit 1 ; \
  fi \
"; }

`safe_source ~/BashLib/.alias`
`safe_source ~/BashLib/.project_configurations`